<Page
    x:Class="MesibaViewer.View.PartyImageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MesibaViewer.View"
    xmlns:ViewModel="clr-namespace:MesibaViewer.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <FlipView ItemsSource="{x:Bind partyImageViewModel.listOfItems}" SelectionChanged="{x:Bind partyImageViewModel.SelectionChanged}"
            DoubleTapped="{x:Bind partyImageViewModel.ResetZoom}" Loaded="{x:Bind partyImageViewModel.SetFirstPic}"/>
        <StackPanel Loaded="{x:Bind partyImageViewModel.LoadLocationBar}" Orientation="Horizontal" VerticalAlignment="Bottom"
            HorizontalAlignment="Center" Height="100" Canvas.ZIndex="1" Background="White"/>
    </Grid>
</Page>
